spring:
  datasource:
#    url: jdbc:h2:tcp://localhost/~/dcloud
    url: jdbc:mysql://localhost:3306/dcloud?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
#    username: sa
    username: root
#    password:
    password: cjsak123
#    driver-class-name: org.h2.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
  h2:
    console:
      enabled: true
  redis:
    port: 6379
    host: localhost
jwt:
  secret: testtesttesttesttesttesttesttest
logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace

cloud:
  aws:
    stack:
      auto: false